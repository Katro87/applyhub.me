export function App() {
  return (
    <main
      style={{
        minHeight: '100vh',
        display: 'grid',
        placeItems: 'center',
        margin: 0,
        fontFamily: 'Inter, system-ui, -apple-system, Segoe UI, Roboto, sans-serif',
        padding: '1.5rem',
        textAlign: 'center',
      }}
    >
      <section>
        <h1 style={{ marginBottom: '0.5rem' }}>ApplyHub</h1>
        <p style={{ margin: 0, color: '#4b5563' }}>
          Home screen is now loading correctly.
        </p>
      </section>
    </main>
  );
}
